[project]
name = "rideday-radar"
version = "0.1.0"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["live: hits real websites (deselect with -m 'not live')"]
addopts = "-m 'not live'"
pythonpath = ["."]
